# --- Dependencies ---
node_modules/
jspm_packages/

# --- Thêm cái này vào cuối file .gitignore nãy tui đưa ---
backend/config/service-account.json
*.json
!package.json
!package-lock.json
# --- Environment Variables (RẤT QUAN TRỌNG: Không để lộ mật khẩu Database) ---
.env

# --- Browser Profile Data (Não của Tiên - Cực nặng và chứa thông tin cá nhân) ---
backend/gemini-profile-data/
**/Default/Cache/
**/Default/Code Cache/
**/Default/GPUCache/
*.lock
SingletonLock

# --- Build outputs ---
dist/
build/
.next/
out/

# --- Zipped files (Mấy file ship hàng này không nên đưa lên Git) ---
*.zip
*.tar.gz
*.rar

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# --- OS specific files ---
.DS_Store
Thumbs.db
desktop.ini

# --- Playwright/Browsers ---
.cache/
ms-playwright/